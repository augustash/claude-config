# Credentials and pulled data must NEVER be committed to this shared package.
# Site dirs belong OUTSIDE this repo, but ignore these names defensively anyway.
*.env
!*.env.example
nr.env
*/nr.env
out/
*/out/
*.json
# per-site report inputs (the .example versions are committed)
breakpoints.csv
*/breakpoints.csv
intro.html
*/intro.html
